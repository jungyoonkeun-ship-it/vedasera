/* VEDA SERA — site configuration.
   Paste your Meta Pixel ID (from Events Manager) below, commit, push.
   Until then, pixel calls are safely skipped. */
window.VS_CONFIG = {
  pixelId: "REPLACE_WITH_PIXEL_ID",
};
